import React from 'react';
import { View, Button } from 'react-native';

const SplashScreen = () => {
    return (
        <View>
            <Button tittle = " Go to login screen " onPress={() => { /* Handle button press */ }}/>
        </View>
    );
};

export default SplashScreen;